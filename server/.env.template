# BookBox - Variables de Entorno
# IMPORTANTE: Renombrar este archivo a .env y completar con valores reales
# NO incluir este archivo en el repositorio Git (agregar .env a .gitignore)

# Configuración del servidor
PORT=5000
NODE_ENV=development  # Cambiar a 'production' en entorno de producción
CLIENT_URL=http://localhost:19000  # URL del cliente (cambiar en producción)

# JWT (JSON Web Token)
# IMPORTANTE: Generar un secreto seguro y complejo para producción
# Puedes utilizar: require('crypto').randomBytes(64).toString('hex')
JWT_SECRET=tu_jwt_secret_seguro_y_complejo

# Base de datos MongoDB
MONGODB_URI=mongodb://localhost:27017/booktracker
# Para MongoDB Atlas: mongodb+srv://<username>:<password>@cluster.mongodb.net/booktracker

# Cloudinary (para almacenamiento de imágenes)
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret

# Configuración de correo electrónico (Gmail)
EMAIL_USER=tu_email@gmail.com
EMAIL_PASS=tu_contraseña_de_aplicacion_gmail

# Variables adicionales
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutos en milisegundos
# RATE_LIMIT_MAX=100  # Máximo número de solicitudes por ventana
